[tool]
id = "com.thelightphone.helpyytasks"
label = "Helpyy Tasks"
versionCode = 1
versionName = "0.1.0"
permissions = ["android.permission.INTERNET"]
capabilities = []
# This tool targets the LP3. Flip to the emulator value to run it in an AVD.
# serverPackage = "com.thelightphone.sdk.emulator"
serverPackage = "com.lightos"
# The LP3 screen is near-square and every layout here is a vertical list.
orientation = "portrait"
